using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Net.Sockets;
using System.IO;
using System.Threading;

namespace SocketExample
{
    class Program
    {
        static void Main(string[] args)
        {
        //    TcpClient client = new TcpClient();
         //   client.Connect("localhost", 1231);
         //   NetworkStream stream = client.GetStream();

           // StreamReader reader = new StreamReader(stream);
           // string result = reader.ReadToEnd();
          //  Console.WriteLine(result);

          //  byte[] sendbytes = Encoding.ASCII.GetBytes("ENQUIRY.SELECT,,SSICKLES/pa$$w0rd,CUSTOMER-LIST");
         //   stream.Write(sendbytes, 0, sendbytes.Length);            
        //    stream.Flush();

            //stream.Read(result, 0, client.ReceiveBufferSize);
          //  stream.Flush();
            //Console.WriteLine(Encoding.ASCII.GetString(result));
          //  Console.ReadLine();

           // TODO Auto-generated method stub
   
        }
    }
}
